package PersonPackage;

class Employee extends Person {
    private int SALARY;

    Employee(String name, int age, String phoneNumber,String username,String password) {
        super(name, age, phoneNumber,username,password);
    }
}
